const baseStyle = {};

const sizes = {};

const variants = {
  mobileOpen: {
    parts: ['dialog, dialogContainer', 'body'],
    dialog: {
      backgroundColor: 'blue',
      top: '57px',
    },
  },
};

const defaultProps = {};

export default {
  baseStyle,
  variants,
  sizes,
  defaultProps,
};
