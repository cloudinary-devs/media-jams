/** @jsxRuntime classic */
/** @jsx jsx */
import { jsx } from '@udecode/slate-plugins-test-utils';

// We can use this to set the initial body when a note was already created
export const initialValueForcedLayout = () => (
  <fragment>
    <hp>Type your notes here...</hp>
  </fragment>
);
